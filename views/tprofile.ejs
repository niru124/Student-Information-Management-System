<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Profile Page</title>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">

    <div class="bg-white rounded-lg shadow-lg p-8 w-96">
        <div class="flex flex-col items-center">
            <img src="https://www.pngall.com/wp-content/uploads/5/Profile-Male-PNG.png" alt="Profile Picture" class="rounded-full w-32 h-32 mb-4">
            <h1 class="text-2xl font-bold mb-2">User Profile</h1>
        </div>
        
        <% details.forEach((user) => { %>
        <div class="mt-4">
            <div class="mb-2">
                <span class="font-semibold">Username:</span>
                <span id="username" class="font-medium border border-gray-300 rounded-lg p-2 w-full bg-gray-100"><%= user.username %></span>
            </div>
            <div class="mb-2">
                <span class="font-semibold">Name:</span>
                <input type="text" id="name" value="<%= user.name %>" readonly class="border border-gray-300 rounded-lg p-2 w-full bg-gray-100" />
            </div>
            <div class="mb-2">
                <span class="font-semibold">Email:</span>
                <input type="email" id="email" value="<%= user.email %>" readonly class="border border-gray-300 rounded-lg p-2 w-full bg-gray-100" />
            </div>
            <div class="mb-2">
                <span class="font-semibold">Phone:</span>
                <span id="phone" class="border border-gray-300 rounded-lg p-2 w-full m-2 bg-gray-100"><%= user.phone %></span>
            </div>
            <div class="mb-2">
                <span class="font-semibold">Department:</span>
                <span id="department" class="border border-gray-300 rounded-lg m-2 p-2 w-full bg-gray-100"><%= user.department %></span>
            </div>
            <div class="mb-2">
                <span class="font-semibold">Password:</span>
                <span id="password" class="border border-gray-300 rounded-lg p-2 m-2 w-full bg-gray-100">********</span>
            </div>
        </div>
        <% }) %>
        <!-- Edit Button -->
        <div class="mt-6">
            <a href="/teachers/ctprofile" class="block text-center bg-blue-600 text-white font-semibold rounded-lg py-2 hover:bg-blue-500 transition">
                Edit Profile
            </a>
        </div>
    </div>
</body>
</html>
